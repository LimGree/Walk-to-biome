# UiRuntime

**Файл:** `Assets/Script/Core/UI/UiRuntime.cs`

Скрытый объект «хост поверх всех панелей»: сюда кладут [[UiModal]] и тосты, чтобы они были выше карты и паузы.

`Ensure()` создаёт его один раз.
