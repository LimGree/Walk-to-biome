# ResearchUI

**Файл:** `Assets/Script/Core/UI/ResearchUI.cs`

Полноэкранное дерево исследований (клавиша Research). Карточки узлов, требования, кнопка «начать».

То же дерево чуть компактнее живёт во вкладке лаборатории ([[MachineUI]]). Логика одна — [[ResearchSystem]].
