# UiTheme

**Файл:** `Assets/Script/Core/UI/UiTheme.cs`

Старые цвета/шрифты для **uGUI** (Canvas). Большая часть игры уже на UI Toolkit ([[IndustryUi]] + USS). Файл жив, если какой-то префаб кнопки ещё на Image/Text.

Не путать с `IndustryTheme.tss` в Resources.
