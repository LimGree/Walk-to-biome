# BuildIoArrowVisualizer

**Файл:** `Assets/Script/Core/Player/BuildIoArrowVisualizer.cs`

Выключен. Раньше рисовал свои стрелки на полу в режиме стройки — поверх стрелок на префабах получались дубли (2 на вход, 2/4/6 на выходы).

Стрелки I/O теперь только те, что стоят на сокетах: [[SocketArrow]] (`IoArrow_In` / `IoArrow_Out`).
