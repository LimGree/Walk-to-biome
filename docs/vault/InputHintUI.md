# InputHintUI

**Файл:** `Assets/Script/Core/UI/InputHintUI.cs`

Полоска снизу: «ЛКМ поставить · Q повернуть». Набор подсказок зависит от контекста (стройка, карта, пауза, вставка группы).

Тексты клавиш — [[KeybindStore.Hint]]. Можно выключить в общих настройках (`HintsEnabled`).
